# Firebase Admin service account (must match the official Android APK Firebase project).
# Download from Firebase Console → Project settings → Service accounts → Generate new private key.
VELAVPS_FCM_SERVICE_ACCOUNT_PATH=/home/ubuntu/velavps/firebase-adminsdk.json
